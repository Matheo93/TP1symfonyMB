{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>User</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ user.username }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_john_index') }}" class="btn btn-secondary">back to list</a>

    <a href="{{ path('app_john_edit', {'id': user.id}) }}" class="btn btn-primary">edit</a>

    {{ include('john/_delete_form.html.twig') }}
{% endblock %}